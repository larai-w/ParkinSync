# Note (local dev on older macOS/Python): if the transitive `cryptography`
# dependency fails to build from source, pre-install a binary wheel first:
#   pip install 'cryptography<44'

# Runtime dependencies (bundled into the Lambda deployment package)
requests>=2.32
google-api-python-client>=2.190
google-auth>=2.40
google-auth-httplib2>=0.2

# Local development / testing only (provided by the AWS Lambda runtime itself)
boto3>=1.34

# Design tooling (design/generate_word.py)
python-docx>=1.1
